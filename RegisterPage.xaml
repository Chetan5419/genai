<Page x:Class="jpmc_genai.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:jpmc_genai"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="#0F1419"
      Title="RegisterPage">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <local:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter" />
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid>
        <!-- Left Panel - Branding -->
        <StackPanel Background="#1a2f4a" VerticalAlignment="Stretch" HorizontalAlignment="Left" Width="300" Margin="0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,80,0,0">
                <TextBlock Text="jpmcAI" Foreground="#00D4FF" FontSize="36" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10" />
                <TextBlock Text="Test Automation Platform" Foreground="#AAAAAA" FontSize="14" TextAlignment="Center" Margin="0,0,0,40" />
                <TextBlock Text="Create your account to get started with intelligent test execution and management." 
                           Foreground="#888888" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" LineHeight="20" />
            </StackPanel>
        </StackPanel>
        
        <!-- Right Panel - Registration Form -->
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="400" Margin="0,0,60,0">
            <TextBlock Text="Create Account" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,10" />
            <TextBlock Text="Join jpmcAI and start automating your tests" Foreground="#AAAAAA" FontSize="13" Margin="0,0,0,30" />
            
            <!-- Name Field -->
            <TextBlock Text="Full Name" Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Grid Margin="0,0,0,15">
                <TextBox x:Name="NameTextBox" />
                <TextBlock Text="John Doe" Style="{StaticResource PlaceholderStyle}"
                           Visibility="{Binding Text, ElementName=NameTextBox, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />
            </Grid>
            
            <!-- Email Field -->
            <TextBlock Text="Email Address" Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Grid Margin="0,0,0,15">
                <TextBox x:Name="MailTextBox" />
                <TextBlock Text="john@example.com" Style="{StaticResource PlaceholderStyle}"
                           Visibility="{Binding Text, ElementName=MailTextBox, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />
            </Grid>
            
            <!-- Password Field -->
            <TextBlock Text="Password" Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Grid Margin="0,0,0,15">
                <PasswordBox x:Name="PasswordBox" />
                <TextBlock x:Name="PasswordPlaceholder" Text="Enter a strong password" Style="{StaticResource PlaceholderStyle}" />
            </Grid>
            
            <!-- Role Field -->
            <TextBlock Text="Role" Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Grid Margin="0,0,0,25">
                <TextBox x:Name="RoleTextBox" />
                <TextBlock Text="QA Engineer" Style="{StaticResource PlaceholderStyle}"
                           Visibility="{Binding Text, ElementName=RoleTextBox, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />
            </Grid>
            
            <!-- Register Button -->
            <Button Content="Create Account" Click="SaveButton_Click" Margin="0,0,0,12" Height="40" FontSize="14" FontWeight="SemiBold" />
            
            <!-- Back to Login Link -->
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock Text="Already have an account? " Foreground="#AAAAAA" FontSize="12" />
                <Button Content="Sign In" Click="BackButton_Click" Background="Transparent" Foreground="#00D4FF" 
                        BorderThickness="0" Padding="0" FontSize="12" FontWeight="Bold" Cursor="Hand" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
